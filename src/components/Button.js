import React from 'react'

function Button(props) {
  return (
    <div>
  {/* <button onClick={props.callApi}>next joke pls</button> */}
  <button onClick={props.callApi}>next</button>

    </div>
  )
}

export default Button